<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/menu_main"
        android:icon="@drawable/ic_edit_primary_24dp"
        android:orderInCategory="0"
        android:title="menu"
        app:showAsAction="always">
        <menu>
            <item
                android:icon="@drawable/ic_today_primary_24dp"
                android:id="@+id/edit_date"
                android:title="EDIT DATE"/>

            <item
                android:icon="@drawable/ic_accessibility_primary_24dp"
                android:id="@+id/edit_name"
                android:title="EDIT NAME" />

            <item
                android:icon="@drawable/ic_delete_primary_24dp"
                android:id="@+id/delete"
                android:title="DELETE" />
        </menu>
    </item>

</menu>